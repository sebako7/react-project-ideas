// jest-dom adds custom matchers (e.g. toBeInTheDocument) for asserting on DOM nodes.
// CRA auto-loads this file before each test file (configured by react-scripts).
import "@testing-library/jest-dom";
